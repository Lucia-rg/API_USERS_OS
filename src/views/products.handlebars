<div class="container mt-4">
    <div class="row">
        <div class="col-12">
            <!-- Header con información del usuario -->
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h1 class="text-secondary">🎵 Productos - Onda Sonar</h1>
                <div class="d-flex align-items-center gap-3">
                    <!-- Badge de rol -->
                    <span class="badge {{#if (eq user.role 'admin')}}bg-secondary{{else}}bg-light text-black{{/if}} fs-6">
                        {{#if (eq user.role 'admin')}}
                        👑 Administrador
                        {{else}}
                        👤 Usuario
                        {{/if}}
                    </span>
                    
                    <!-- Información del usuario -->
                    <div class="text-end">
                        <strong class="d-block">{{user.first_name}} {{user.last_name}}</strong>
                        <small class="text-muted">{{user.email}}</small>
                    </div>
                    
                    <!-- Botones de acción -->
                    <div class="btn-group">
                        <a href="/api/sessions/current" class="btn btn-outline-secondary">
                            👤 Ver Perfil
                        </a>
                        <a href="/api/sessions/logout" class="btn btn-outline-secondary">
                            🚪 Cerrar Sesión
                        </a>
                    </div>
                </div>
            </div>

            <!-- Mensaje de bienvenida -->
            <div class="alert alert-secondary">
                <h4 class="alert-heading">¡Bienvenido/a! 🎉</h4>
                <p class="mb-3 fs-5">{{welcomeMessage}}</p>
                <hr>
                <div class="row">
                    <div class="col-md-4">
                        <strong>📧 Email:</strong> {{user.email}}
                    </div>
                    <div class="col-md-4">
                        <strong>🎂 Edad:</strong> {{user.age}} años
                    </div>
                    <div class="col-md-4">
                        <strong>👤 Rol:</strong> 
                        <span>
                            {{user.role}}
                        </span>
                    </div>
                </div>
                {{#if (eq user.role 'admin')}}
                <div class="mt-2">
                    <small class="text-muted">💼 <strong>Modo Administrador Activado:</strong> Tienes acceso completo al sistema de usuarios.</small>
                </div>
                {{/if}}
            </div>
        </div>
    </div>
</div>
